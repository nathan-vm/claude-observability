name: release
on:
  push:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: release-main
  cancel-in-progress: false

jobs:
  compute-version:
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.compute.outputs.version }}
      bumped: ${{ steps.compute.outputs.bumped }}
      changelog: ${{ steps.compute.outputs.changelog }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Compute next version, update changelog, tag
        id: compute
        # NOTE: this step commits the changelog and pushes the tag BEFORE
        # the build/release jobs run below. If any build job fails after
        # this step succeeds, main is left with a tag + changelog commit
        # but no GitHub Release. Recovery: use "Re-run failed jobs" (NOT
        # "Re-run all jobs") — re-running this step would see the tag it
        # already created, find no new releasable commits, and skip the
        # rebuild entirely, silently abandoning the release.
        run: |
          output="$(.github/scripts/compute-next-version.sh /tmp/changelog-fragment.md)"
          echo "$output" >> "$GITHUB_OUTPUT"
          version=$(echo "$output" | grep '^version=' | cut -d= -f2)
          bumped=$(echo "$output" | grep '^bumped=' | cut -d= -f2)

          if [ "$bumped" = "true" ]; then
            {
              echo "# Changelog"
              echo
              cat /tmp/changelog-fragment.md
              tail -n +3 CHANGELOG.md
            } > /tmp/CHANGELOG.new
            mv /tmp/CHANGELOG.new CHANGELOG.md

            git add CHANGELOG.md
            git commit -m "docs(changelog): v${version} [skip ci]"
            git tag -a "v${version}" -m "Release v${version}"
            git push origin main --follow-tags

            echo "changelog<<CHANGELOG_EOF" >> "$GITHUB_OUTPUT"
            cat /tmp/changelog-fragment.md >> "$GITHUB_OUTPUT"
            echo "CHANGELOG_EOF" >> "$GITHUB_OUTPUT"
          fi

  package:
    needs: compute-version
    if: needs.compute-version.outputs.bumped == 'true'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: windows
            goarch: amd64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: build wizard
        working-directory: src/wizard
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          ext=""
          if [ "${{ matrix.goos }}" = "windows" ]; then ext=".exe"; fi
          go build -ldflags "-X main.version=${{ needs.compute-version.outputs.version }}" -o "claude-observability-wizard-${{ matrix.goos }}-${{ matrix.goarch }}${ext}" ./cmd/wizard

      - name: build collector
        working-directory: src/collector
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          ext=""
          if [ "${{ matrix.goos }}" = "windows" ]; then ext=".exe"; fi
          go build -ldflags "-X main.version=${{ needs.compute-version.outputs.version }}" -o "claude-observability-collector-${{ matrix.goos }}-${{ matrix.goarch }}${ext}" ./cmd/collector

      - name: zip
        run: |
          ext=""
          if [ "${{ matrix.goos }}" = "windows" ]; then ext=".exe"; fi
          zip -j "claude-observability-${{ matrix.goos }}-${{ matrix.goarch }}.zip" \
            "src/wizard/claude-observability-wizard-${{ matrix.goos }}-${{ matrix.goarch }}${ext}" \
            "src/collector/claude-observability-collector-${{ matrix.goos }}-${{ matrix.goarch }}${ext}"

      - uses: actions/upload-artifact@v4
        with:
          name: package-${{ matrix.goos }}-${{ matrix.goarch }}
          path: claude-observability-${{ matrix.goos }}-${{ matrix.goarch }}.zip

  dash-generator:
    needs: compute-version
    if: needs.compute-version.outputs.bumped == 'true'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: windows
            goarch: amd64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
      - name: build
        working-directory: src/dash-generator
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          ext=""
          if [ "${{ matrix.goos }}" = "windows" ]; then ext=".exe"; fi
          go build -ldflags "-X main.version=${{ needs.compute-version.outputs.version }}" -o "claude-observability-dash-generator-${{ matrix.goos }}-${{ matrix.goarch }}${ext}" ./cmd/dash-generator
      - uses: actions/upload-artifact@v4
        with:
          name: binaries-dash-generator-${{ matrix.goos }}-${{ matrix.goarch }}
          path: src/dash-generator/claude-observability-dash-generator-*

  release:
    needs: [compute-version, package, dash-generator]
    if: needs.compute-version.outputs.bumped == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.compute-version.outputs.version }}
          body: ${{ needs.compute-version.outputs.changelog }}
          files: dist/*
